"""
Smoke tests for starter example {{ class_name }}

This verifies the basic demo method returns the expected value.
"""

from {{ module_path }} import {{ class_name }}


def test_demo_returns_ok():
    obj = {{ class_name }}()
    assert obj.demo() == 'ok'


def test_add_and_safe_divide_if_present():
    obj = {{ class_name }}()
    # Optional: if these methods exist in the starter example, verify basic behavior
    if hasattr(obj, "add"):
        assert obj.add(2, 3) == 5
        assert obj.add(0, 0) == 0
    if hasattr(obj, "safe_divide"):
        assert obj.safe_divide(10, 2) == 5.0
        assert obj.safe_divide(1, 0) is None
