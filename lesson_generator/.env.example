# Example environment variables for local development
# Do not put real secrets here. Copy to ".env" and replace placeholders locally.

# OpenAI
OPENAI_API_KEY=your-openai-api-key
# Optional: model override (defaults to gpt-4o-mini in code)
# OPENAI_MODEL=gpt-4o-mini

# Development settings
DEBUG=true
LOG_LEVEL=INFO

# Default lesson generation settings (optional)
DEFAULT_DIFFICULTY=intermediate
DEFAULT_MODULES_COUNT=1
DEFAULT_OUTPUT_DIR=./lesson_generator/.demo_out